# CoinPoker Intelligence Assistant Environment Configuration
# Copy this file to .env and fill in your actual values.
# DO NOT commit your real secrets to version control.

# === REQUIRED SECRETS ===
# AI_API_KEY is required for all external AI/LLM features.
AI_API_KEY=

# === OPTIONAL SECRETS ===
# DB_SECRET is only needed for advanced database features. Leave blank if unused.
DB_SECRET=

# === API Settings ===
# API_BASE_URL is optional. If set, overrides the default API endpoint.
API_BASE_URL=
# API_TIMEOUT in milliseconds (default: 30000)
API_TIMEOUT=30000
# API_RETRIES (default: 3)
API_RETRIES=3

# === Application Settings ===
# NODE_ENV: 'development' or 'production'
NODE_ENV=development
# LOG_LEVEL: debug, info, warn, error (default: info)
LOG_LEVEL=info

# === Window Settings ===
# WINDOW_WIDTH (default: 1200)
WINDOW_WIDTH=1200
# WINDOW_HEIGHT (default: 800)
WINDOW_HEIGHT=800

# === Performance Settings ===
# ENABLE_METRICS: true or false (default: true)
ENABLE_METRICS=true
# PERFORMANCE_SAMPLE_RATE: 0.0 - 1.0 (default: 0.1)
PERFORMANCE_SAMPLE_RATE=0.1
# MAX_MEMORY_USAGE in bytes (default: 536870912 for 512MB)
MAX_MEMORY_USAGE=536870912

# === OCR Configuration ===
# OCR_LANGUAGE: Tesseract language code (default: eng)
OCR_LANGUAGE=eng
# OCR_CONFIDENCE: 0.0 - 1.0 (default: 0.8)
OCR_CONFIDENCE=0.8

# === Documentation ===
# - All variables above are loaded at runtime.
# - Required: AI_API_KEY
# - Optional: DB_SECRET, API_BASE_URL, others have defaults.
# - If a required variable is missing, the app will fail to start with a clear error.
# - Do not commit this file with real secrets.